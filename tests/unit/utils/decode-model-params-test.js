import decodeModelParams from 'arschmitz-weather/utils/decode-model-params';
import { module, test } from 'qunit';

module('Unit | Utility | decode model params');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = decodeModelParams();
  assert.ok(result);
});
