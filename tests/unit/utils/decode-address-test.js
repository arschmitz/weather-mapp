import decodeAddress from 'arschmitz-weather/utils/decode-address';
import { module, test } from 'qunit';

module('Unit | Utility | decode address');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = decodeAddress();
  assert.ok(result);
});
