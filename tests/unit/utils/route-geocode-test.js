import routeGeocode from 'arschmitz-weather/utils/route-geocode';
import { module, test } from 'qunit';

module('Unit | Utility | route geocode');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = routeGeocode();
  assert.ok(result);
});
